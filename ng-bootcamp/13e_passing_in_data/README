uses: isolated scope, directive, restrict, external template

Create a directive that will accept an address argument
and display the formatted address.

You should be able to create multiple
directives and format as many addresses as needed.